{{ __('emails.password_reset_greeting') }}

{{ __('emails.password_reset_body') }}

{{ __('emails.password_reset_button') }}: {{ $resetUrl }}

{{ __('emails.password_reset_expire') }}

{{ __('emails.password_reset_ignore') }}

--
{{ config('app.name') }}
